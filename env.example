VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key
VITE_SPINE_PROXY_URL=https://your-approved-spine-domain.com/ai/docs
VITE_STORAGE_BUCKET=training-docs

# Optional: true allows local Markdown-only chunk fallback if spine indexing is not ready yet.
# Keep false in production when the spine indexer is available.
VITE_ENABLE_LOCAL_CHUNK_FALLBACK=false
